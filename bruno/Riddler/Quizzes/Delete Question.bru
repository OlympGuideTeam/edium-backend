meta {
  name: Delete Question
  type: http
  seq: 5
}

delete {
  url: {{riddlerUrl}}/riddler/v1/quizzes/{{quizId}}/questions/{{questionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
