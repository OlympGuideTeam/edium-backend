meta {
  name: Publish Quiz
  type: http
  seq: 9
}

post {
  url: {{riddlerUrl}}/riddler/v1/quizzes/{{quizId}}/publish
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "is_public": false
  }
}
