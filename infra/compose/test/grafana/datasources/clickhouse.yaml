apiVersion: 1
datasources:
  - name: ClickHouse
    type: grafana-clickhouse-datasource
    uid: edium-clickhouse
    jsonData:
      host: clickhouse
      port: 9000
      username: vector
      defaultDatabase: logs
    secureJsonData:
      password: ${GRAFANA_CLICKHOUSE_PASSWORD}
